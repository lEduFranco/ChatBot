function getGender(name) {
  return 'male';

  /*
  if (name != null) {
      name = name.split(' ')[0]
      let gender = names.find(e => e.name === name)
      var gender_ = ''
      if (gender != undefined) {
          gender_ = gender.gender
      } else {
          gender_ = 'male'
      }
      return gender_
  } else {
      return 'male';
  }*/

}
module.exports = getGender;
